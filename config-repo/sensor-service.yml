spring:
  datasource:
    url: jdbc:h2:mem:sensor_db
    driver-class-name: org.h2.Driver
    username:
    password:
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

server:
  port: 8082

iot:
  api:
    base-url:
  credentials:
    username:
    password:
